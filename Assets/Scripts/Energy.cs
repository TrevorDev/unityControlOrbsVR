using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Energy : MonoBehaviour {
//	public SteamVR_TrackedController left;
//	public SteamVR_TrackedController right;
	// Use this for initialization
	void Start () {
		//var left = GameObject.Find ("[CameraRig]").transform.Find("Controller (left)").GetComponent<SteamVR_TrackedController>();
  //      left.TriggerClicked += (a, b) =>
  //      {
  //          Debug.Log("hit");
  //      };
	}
	
	// Update is called once per frame
	void Update () {
	}
}
